import { Validators } from "@angular/forms";

export const MERCADO_FORMULARIO_COFNG = {
  Nome: ['', Validators.required],
  LinkImage: ['', Validators.required],
  UrlSite: ['', Validators.required]
}
